/* honokajs main */

// Base function.
var honoka = function() {
  // Add functionality here.
  return true;
};


// Version.
honoka.VERSION = '0.1.0';


// Export to the root, which is probably `window`.
root.honoka = honoka;
